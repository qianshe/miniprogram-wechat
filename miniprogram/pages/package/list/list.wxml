<!--pages/package/list/list.wxml-->
<view class="package-list-page">
  <!-- Page Header -->
  <view class="page-header">
    <text class="page-title">精选套餐</text>
  </view>

  <!-- Package List -->
  <scroll-view 
    class="package-scroll"
    scroll-y
    enhanced
    show-scrollbar="{{false}}"
    bindscrolltolower="onReachBottom"
  >
    <!-- Package Cards -->
    <view class="package-cards">
      <!-- [殡葬平台转型] 渲染过滤：兜底过滤非白事套餐 -->
      <view 
        class="package-card"
        wx:for="{{packages}}"
        wx:key="_id"
        wx:if="{{item.type === 'white'}}"
        bindtap="onPackageClick"
        data-id="{{item._id}}"
      >
        <!-- Card Header with Image -->
        <view class="card-header">
          <image 
            class="package-image" 
            src="{{item.thumbSrc || item.imageUrl}}" 
            mode="aspectFill"
            lazy-load="{{true}}"
          />
        </view>

        <!-- Card Body -->
        <view class="card-body">
          <view class="package-title-row">
            <text class="package-name">{{item.name}}</text>
            <view class="item-count-badge">
              <text class="item-count">{{item.itemCount}}项</text>
            </view>
          </view>
          
          <text class="package-desc">{{item.description}}</text>
          
          <!-- Price Section -->
          <view class="price-section">
            <c-price
              price="{{item.price}}"
              size="medium"
            />
          </view>

          <!-- Action Hint -->
          <view class="action-hint">
            <text class="hint-text">点击查看详情</text>
            <view class="arrow-icon">
              <text class="arrow">></text>
            </view>
          </view>
        </view>
      </view>
    </view>

    <!-- Loading States -->
    <view class="loading-section">
      <c-loading loading="{{loading}}" text="加载中..." />

      <view class="no-more" wx:if="{{!loading && !hasMore && packages.length > 0}}">
        <view class="divider-line"></view>
        <text class="no-more-text">已加载全部套餐</text>
        <view class="divider-line"></view>
      </view>
    </view>

    <!-- Empty State -->
    <c-empty
      show="{{isEmpty && !loading}}"
      title="暂无套餐"
      description="暂无可用套餐"
    />

    <!-- Bottom Safe Area -->
    <view class="safe-area-bottom"></view>
  </scroll-view>
</view>
